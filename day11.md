# Day 11 - Bash Scripting Basics

## Scripts Created
- `hello.sh` -- prints a simple greeting
- `greet_user.sh` -- reads user input and prints a personalized message
- `env_info.sh` -- displays system and environment variables

## Notes
- Learned the structure of a Bash script (`#!/bin/bash`, comments, execution)
- Practiced `chmod` to make scripts executable
- Used `read` for user input and environment variables for dynamic info
